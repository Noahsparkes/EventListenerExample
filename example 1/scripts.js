document
       .querySelector('#example')
       .addEventListener('click',() => console.log('hello')) //logs hello when the button is clicked


window.addEventListener('scroll', () => console.log('scrolled')); // logs 'scrolled' when the scrolling action is used 
 