const BACKGROUNDS =[
    'red',
    'blue',
    'green',
    'purple',
    'brown',
    'black',
]

const buttonsList = document.querySelector('button')
const buttonsArray = Array.from(buttonList)

/**
 *  Returns a random color value, based on a random index used on the
 * {@link BACKGROUNDS}
 */
 
const calcRandomColor = () => {
    
}